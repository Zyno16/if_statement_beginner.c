#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x,y;
   printf("enter x ?:");
   scanf("%d",&x);
   printf("enter y ?:");
   scanf("%d",&y);
   if(x>=0&&y>=0)printf("the numbers have same sign");
   else printf("the numbers does'y have same sign");


    return 0;
}
